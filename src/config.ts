/**
 * Центральный конфиг баланса игры.
 * Все числа (стоимости, параметры добычи, стартовые значения) — здесь.
 */
export const CONFIG = {
    // Стартовые ресурсы команд
    startCredits: 100,

    // Пассивный доход в секунду (для баланса: чтобы игра не замирала)
    passiveIncomePerSec: 1,

    // Стоимости юнитов и построек
    costs: {
        tank: 50,
        builder: 25,
        factory: 100,
        harvester: 75
    },

    // Параметры квантовой добычи ресурсов
    harvest: {
        // Базовое количество добычи за тик (случайный диапазон: min..max)
        baseMin: 3,
        baseMax: 8,
        // Интервал между тиками добычи (мс)
        tickInterval: 1200,
        // Множитель добычи, когда харвестер внутри облака вероятности
        cloudMultiplier: 2,
        // Шанс сбоя при добыче внутри облака (в процентах)
        cloudMisfireChance: 25,
        // Урон харвестеру при сбое в облаке
        cloudMisfireDamage: 10,
        // Сколько ресурсов теряется при сбое (процент от добытого)
        cloudMisfireLossPct: 0.5,
        // Радиус, в котором харвестер ищет жилу
        findRange: 250,
        // Радиус «сбора»: расстояние до жилы, при котором идёт добыча
        harvestRange: 120
    },

    // Квантовые жилы ресурсов
    resourceFields: {
        // Сколько жил спавнить на карте
        count: 3,
        // Радиус зоны добычи жилы
        radius: 70
    },

    // ИИ противника (команда 2)
    ai: {
        // Задержка перед стартом ИИ (мс)
        startDelay: 10000,
        // Интервал между «тиками» принятия решений (мс)
        thinkInterval: 2000,
        // Сколько танков накопить перед атакой
        attackThreshold: 3,
        // Максимум фабрик, которые ИИ строит
        maxFactories: 2,
        // Сколько харвестеров ИИ держит
        harvesters: 2,
        // Сколько танков за раз заказывать у фабрики
        productionBatch: 2
    }
};
